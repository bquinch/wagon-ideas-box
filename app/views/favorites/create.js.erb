function addBookmark(innerHTML) {

  const newBookmark = document.querySelector("[data-fav='<%= @idea.id %>']");
  newBookmark.innerHTML = innerHTML;
};

addBookmark('<%= j render "shared/add-to-fav", favorite: @idea.favorites.find_by(user: current_user) %>');
<%= render 'flashes' %>
